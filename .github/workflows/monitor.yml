name: Websiter Monitor

on:
  schedule:
    # 每周一上午9点（北京时间）
    - cron: '0 1 * * 1'
  workflow_dispatch:

permissions:
  contents: write
  issues: write

jobs:
  monitor:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
      
    - uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        pip install requests beautifulsoup4 lxml
    
    - name: Run monitor
      id: monitor
      run: |
        python monitor.py
    
    - name: Commit state file
      if: success()
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add last_state.json
        git diff --quiet && git diff --staged --quiet || git commit -m "Update monitoring state [skip ci]"
        git push
    
    - name: Create Issue for updates
      if: success()
      run: |
        # 检查是否有更新
        if grep -q "发现更新" report.md 2>/dev/null || grep -q "网站事件更新通知" report.md 2>/dev/null; then
          echo "Found updates, creating issue..."
          
          # 使用GitHub CLI创建Issue
          gh issue create \
            --title "🔔 [$(date +%Y-%m-%d)] 检测到网站更新" \
            --body-file report.md \
            --label "events-update,automated" \
            || echo "Issue creation failed"
        else
          echo "No updates found, skipping issue creation"
        fi
      env:
        GH_TOKEN: ${{ github.token }}
